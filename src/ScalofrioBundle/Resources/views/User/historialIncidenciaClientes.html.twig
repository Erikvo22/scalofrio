{% extends 'base.html.twig' %}

{% block body %}
    {{ include('menu.html.twig') }}
    {{ parent() }}

    {%  for flashMessage in app.session.flashbag.get('mensaje') %}
    <div class="alert alert-success" role="alert">
        <div class="container">{{ flashMessage }}</div>
    </div>
    {% endfor %}

    <div class="container">
        <div class="col-md-12">
            <div class="page-header margin-none">
                {% if is_granted('ROLE_ADMIN') %}
                    <h2><b><span class="glyphicon glyphicon-envelope"></span> Incidencias recibidas </b></h2>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                <h2><b>Incidencias</b></h2>
                    <a href="{{ path('scalofrio_incidenciaCliente_add') }}" class="btn btn-sm btn-warning padding-none">
                        <span class="glyphicon glyphicon-ok-circle"></span>
                        Reportar incidencia
                    </a>
                {% endif %}
            </div>
            <div class="col-md-12">
            {% if pagination|length > 0 %}
                 <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <tr>
                        <th>ID</th>
                        <th>TITULO</th>
                        <th>TIPO</th>
                        <th>PRIORIDAD</th>
                        <th>ESTADO</th>
                        <th>ESTABLECIMIENTO</th>
                        <th>FECHA INCIDENCIA</th>
                        <th>ACCIONES</th>
                    </tr>
                    {% for incidencia in pagination %}
                        <tr>
                            <td>{{ incidencia.id }}</td>
                            <td>{{ incidencia.titulo}}</td>
                            <td>{{ incidencia.tipo }}</td>
                            <td>{{ incidencia.prioridad }}</td>
                            {% if incidencia.estado == 0 %}
                            <td>Pendiente</td>
                            {% endif %}
                            {% if incidencia.estado == 1 %}
                            <td>Resuelto</td>
                            {% endif %}
                            <td>{{ incidencia.establecimiento }}</td>
                            <td>{{ incidencia.fechaIncidencia|date('d-m-Y') }}</td>
                            <td class="actions">
                                <a href="{{ path('scalofrio_incidenciaCliente_show', { id: incidencia.id}) }}" class="btn btn-sm btn-primary">Ver</a>
                                {% if is_granted('ROLE_ADMIN') %}
                                     <a href="{{ path('scalofrio_incidenciaActualizarEstado', { id: incidencia.id }) }}" class="btn btn-sm btn-success">Cambiar Estado</a>
                                {% endif %}
                            </td>
                        </tr>
                    {% endfor %}
                </table>    
                {% else %}
                <div style="text-align:center;">
                    <span><i class="fas fa-server" style="font-size:64px"></i></span>
                    <span><h4>No se ha generado ninguna incidencia.</h4></span>    
                </div>
                {% endif %}
                <div class="navigation" style="text-align: center">
                    {{ knp_pagination_render(pagination) }}
                </div>
            </div>
            </div>
        </div>
    </div>
{% endblock %}


